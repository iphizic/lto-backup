# 📼 LTO Backup System (Professional Edition)

Профессиональная система резервного копирования на ленточные накопители LTO-4 и выше. 
Объектно-ориентированное решение с поддержкой многотомности, автоматической оптимизацией, 
безопасной работой с файлами и комплексным мониторингом ресурсов.

## 🚀 Быстрый старт

### Установка системных зависимостей
```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y tar mt-st mtx mbuffer smartctl curl lsscsi tapeinfo python3 python3-pip

# CentOS/RHEL
sudo yum install -y tar mt-st mtx mbuffer smartmontools curl lsscsi python3 python3-pip
```

### Установка Python зависимостей
```bash
pip install pyyaml jsonschema psutil chardet requests
```

### Клонирование и настройка
```bash
git clone <repository-url>
cd lto_backup_system

# Создание конфигурации по умолчанию
python3 -c "from modules.config_manager import create_default_config; create_default_config('config.yaml')"

# Настройка конфигурации
nano config.yaml  # Отредактируйте настройки под вашу систему

# Проверка системы
python3 lto_main.py status
```

## 📁 Структура проекта

```
lto_backup_system/
├── config.yaml                    # Основная конфигурация (YAML)
├── lto_main.py                    # Основной CLI интерфейс
├── modules/                       # Модули системы
│   ├── __init__.py
│   ├── config_manager.py          # Управление конфигурацией YAML/JSON
│   ├── file_utils.py              # Безопасная работа с файлами и кодировками
│   ├── system_monitor.py          # Мониторинг ресурсов и оптимизация
│   ├── tape_drive.py              # Класс для работы с ленточными накопителями
│   ├── backup_job.py              # Управление задачами бэкапа/восстановления
│   ├── lto_logger.py              # Централизованное логирование с ротацией
│   ├── registry_manager.py        # Управление реестром бэкапов
│   └── core_tg.py                 # Безопасные уведомления в Telegram
├── scripts/                       # Вспомогательные скрипты
│   ├── log_manager.py             # Управление логами
│   ├── setup_security.py          # Настройка безопасности
│   ├── test_logging.py            # Тестирование системы логирования
│   └── check_deps.sh              # Проверка системных зависимостей
├── logs/                          # Директория логов (автосоздается)
│   ├── lto_system.log             # Основные события системы
│   ├── lto_errors.log             # Ошибки и критические события
│   ├── lto_tape.log               # Операции с лентой
│   └── lto_performance.log        # Метрики производительности
├── manifests/                     # Файлы манифестов бэкапов
├── backups/                       # Резервные копии реестра
└── README.md                      # Эта документация
```

## ⚙️ Конфигурация

### Формат конфигурации
Система использует **YAML** или **JSON** формат конфигурации. INI формат не поддерживается.

### Пример config.yaml
```yaml
database:
  registry_file: "backup_registry.csv"
  manifest_dir: "./manifests"
  backup_dir: "./backups"
  log_dir: "./logs"
  cache_dir: "./cache"

hardware:
  tape_device: "/dev/nst0"
  robot_enabled: false
  robot_device: "/dev/sg3"
  error_threshold: 50
  auto_clean: true
  clean_interval_hours: 24

buffer:
  size: "2G"
  fill_percent: "90%"
  block_size: "256k"
  auto_adjust: true
  min_size: "512M"
  max_size: "4G"

notification:
  telegram_enabled: false
  telegram_token: "ВАШ_ТОКЕН_БОТА"
  telegram_chat_id: "ВАШ_ID_ЧАТА"
  email_enabled: false
  email_smtp_server: "smtp.example.com"
  email_smtp_port: 587
  email_username: ""
  email_password: ""
  email_recipients: []

backup:
  default_excludes:
    - "/proc"
    - "/sys"
    - "/dev"
    - "/run"
    - "/tmp"
    - "*.log"
    - "*.tmp"
    - "*.temp"
    - "*.cache"
  compress_before_backup: false
  encryption_enabled: false
  encryption_key: ""
  verify_after_backup: true
  max_backup_age_days: 365
  retention_policy: "yearly"

logging:
  level: "INFO"
  console_enabled: true
  file_enabled: true
  max_file_size: 10485760
  backup_count: 7
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  date_format: "%Y-%m-%d %H:%M:%S"
```

### Создание конфигурации по умолчанию
```bash
# Создать конфигурацию YAML
python3 -c "from modules.config_manager import create_default_config; create_default_config('config.yaml')"

# Создать конфигурацию JSON
python3 -c "from modules.config_manager import create_default_config, ConfigFormat; create_default_config('config.json', ConfigFormat.JSON)"
```

## 🎯 Основные команды

### Создание бэкапа
```bash
python3 lto_main.py backup /путь/к/данным "Метка_бэкапа"
```
**Опции:**
- `--verify` - проверить целостность после записи
- `--compress` - включить сжатие

### Восстановление данных
```bash
python3 lto_main.py restore /путь/восстановления "Метка_бэкапа"
```
**Опции:**
- `--tape` - указать номер ленты для восстановления

### Просмотр статуса системы
```bash
python3 lto_main.py status
```
Выводит информацию о:
- Состоянии системы (память, диск, CPU)
- Ленточных устройствах
- Конфигурации
- Готовности к работе

### Список бэкапов
```bash
python3 lto_main.py list --type backup
python3 lto_main.py list --type tape
python3 lto_main.py list --type job
```

### Проверка целостности
```bash
python3 lto_main.py verify "Метка_бэкапа"
```

### Чистка ленточного накопителя
```bash
python3 lto_main.py clean
python3 lto_main.py clean --force  # Принудительная чистка
```

### Управление конфигурацией
```bash
python3 lto_main.py config --show      # Показать конфигурацию
python3 lto_main.py config --validate  # Проверить валидность
python3 lto_main.py config --export yaml  # Экспортировать в YAML
python3 lto_main.py config --export json  # Экспортировать в JSON
```

## 🔧 Утилиты управления

### Управление логами (log_manager.py)
```bash
# Показать информацию о логах
python3 scripts/log_manager.py show

# Просмотреть конец лог-файла
python3 scripts/log_manager.py tail lto_system.log
python3 scripts/log_manager.py tail lto_system.log -n 100  # 100 строк

# Поиск по логам
python3 scripts/log_manager.py search "ERROR"
python3 scripts/log_manager.py search "backup" -i  # Без учета регистра

# Очистка старых логов
python3 scripts/log_manager.py cleanup
python3 scripts/log_manager.py cleanup --days 7  # Удалить логи старше 7 дней

# Изменение уровня логирования
python3 scripts/log_manager.py level DEBUG
python3 scripts/log_manager.py level INFO
python3 scripts/log_manager.py level WARNING
```

### Настройка безопасности
```bash
# Проверка и настройка безопасности системы
python3 scripts/setup_security.py
```

### Тестирование компонентов
```bash
# Тестирование системы логирования
python3 scripts/test_logging.py

# Тестирование работы с лентой
python3 -m modules.tape_drive

# Тестирование мониторинга ресурсов
python3 -m modules.system_monitor

# Тестирование работы с файлами
python3 -m modules.file_utils
```

## 🛡️ Безопасность

### Особенности безопасности системы:
1. **Маскировка чувствительных данных** - токены и пароли скрываются в логах
2. **Проверка прав доступа** - автоматическая проверка прав к файлам и устройствам
3. **Валидация входных данных** - все параметры проверяются перед выполнением
4. **Безопасное выполнение команд** - таймауты, обработка ошибок, безопасные оболочки
5. **Автоматическое определение кодировок** - безопасная работа с файлами в разных кодировках

### Настройка Telegram уведомлений:
1. Получите токен бота у [@BotFather](https://t.me/BotFather)
2. Узнайте ваш Chat ID (можно получить у [@userinfobot](https://t.me/userinfobot))
3. Добавьте в конфигурацию:
```yaml
notification:
  telegram_enabled: true
  telegram_token: "ВАШ_ТОКЕН"
  telegram_chat_id: "ВАШ_CHAT_ID"
```

## 📊 Мониторинг и оптимизация

### Автоматическая оптимизация
Система автоматически:
1. **Проверяет доступную память** перед запуском операций
2. **Оптимизирует размер буфера mbuffer** под текущую нагрузку
3. **Мониторит загрузку системы** и корректирует параметры
4. **Проверяет свободное место на диске** для временных файлов

### Ручная настройка производительности
```yaml
buffer:
  size: "2G"           # Размер буфера (автоподбор при auto_adjust: true)
  fill_percent: "90%"  # Процент заполнения буфера перед записью
  block_size: "256k"   # Размер блока
  auto_adjust: true    # Автоматическая регулировка
  min_size: "512M"     # Минимальный размер буфера
  max_size: "4G"       # Максимальный размер буфера
```

## 🔄 Работа с лентой

### Поддерживаемые операции:
- **Автоматическое определение устройств** - система сама находит ленточные накопители
- **Проверка состояния ленты** - статус, позиция, необходимость чистки
- **Многотомные бэкапы** - автоматическая смена лент при заполнении
- **Позиционирование** - точное позиционирование на нужном файле ленты
- **Автоматическая чистка** - уведомление при необходимости чистки накопителя

### Ручное управление лентой:
```bash
# Создать экземпляр ленточного устройства
python3 -c "
from modules.tape_drive import TapeDrive
tape = TapeDrive('/dev/nst0')

# Получить статус
info = tape.get_status()
print(f'Устройство: {info.device}')
print(f'Статус: {info.status.value}')
print(f'Файл: {info.file_number}')
print(f'Чистка требуется: {info.cleaning_required}')

# Перемотать ленту
tape.rewind()

# Перейти к файлу 5
tape.seek_to_file(5)
"
```

## 📁 Работа с файлами и кодировками

### Особенности:
1. **Автоматическое определение кодировок** - UTF-8, CP1251, KOI8-R и др.
2. **Безопасные пути** - обработка путей с не-ASCII символами
3. **Манифесты в UTF-8** - гарантированно корректное отображение любых символов
4. **Резервное копирование реестра** - автоматические бэкапы и восстановление

### Пример работы с файлами:
```python
from modules.file_utils import SafeFileHandler, ManifestProcessor

# Безопасное чтение файла с автоопределением кодировки
content = SafeFileHandler.read_file("/путь/к/файлу.txt")

# Безопасная запись файла в UTF-8
SafeFileHandler.write_file("/путь/выход.txt", content, ensure_utf8=True)

# Создание манифеста
ManifestProcessor.create_manifest(
    "manifest.txt",
    ["/file1.txt", "/file2.txt"],
    label="Мой_бэкап"
)
```

## 🐛 Поиск и устранение неисправностей

### Проверка системы
```bash
# Полная проверка системы
python3 lto_main.py status

# Проверка ленточного устройства
python3 -m modules.tape_drive

# Проверка ресурсов
python3 -m modules.system_monitor

# Проверка логов
python3 scripts/log_manager.py show
python3 scripts/log_manager.py search "ERROR"
```

### Частые проблемы и решения:

#### 1. "Устройство /dev/nst0 не найдено"
```bash
# Проверить наличие устройства
ls -la /dev/nst*

# Проверить права
sudo chmod 666 /dev/nst0

# Добавить пользователя в группу tape
sudo usermod -a -G tape $USER
```

#### 2. "Недостаточно памяти для буфера"
- Уменьшите размер буфера в конфигурации: `buffer.size: "1G"`
- Включите автоматическую регулировку: `buffer.auto_adjust: true`
- Освободите память перед запуском бэкапа

#### 3. "Ошибка кодировки файла"
Система автоматически определяет и конвертирует кодировки. 
Для принудительной конвертации:
```python
from modules.file_utils import SafeFileHandler
SafeFileHandler.convert_file_encoding("вход.txt", "выход.txt", "cp1251", "utf-8")
```

#### 4. "Лента требует чистки"
```bash
# Выполнить чистку
python3 lto_main.py clean

# Принудительная чистка
python3 lto_main.py clean --force
```

### Включение отладочного режима
```bash
# Изменить уровень логирования на DEBUG
python3 scripts/log_manager.py level DEBUG

# Выполнить операцию
python3 lto_main.py backup /test "Тестовый_бэкап"

# Просмотреть подробные логи
tail -f logs/lto_system.log
tail -f logs/lto_debug.log
```

## 📈 Производительность

### Рекомендации по настройке:

#### Для систем с 8ГБ+ RAM:
```yaml
buffer:
  size: "2G"
  min_size: "1G"
  max_size: "3G"
  auto_adjust: true
```

#### Для систем с 4ГБ RAM:
```yaml
buffer:
  size: "1G"
  min_size: "512M"
  max_size: "1.5G"
  auto_adjust: true
```

#### Оптимизация исключений:
```yaml
backup:
  default_excludes:
    - "/proc"
    - "/sys"
    - "/dev"
    - "/run"
    - "/tmp"
    - "*.tmp"
    - "*.temp"
    - "*.cache"
    - "*.log"
    - "/var/cache"
    - "/var/tmp"
```

### Мониторинг производительности:
```bash
# Просмотр метрик производительности
tail -f logs/lto_performance.log

# Статистика выполнения задач
python3 -c "
from modules.backup_job import JobManager
manager = JobManager()
for job in manager.list_jobs():
    print(f'{job[\"job_id\"]}: {job[\"status\"]}')
"
```

## 🔄 Восстановление при сбоях

### Восстановление реестра:
```bash
# Автоматическое восстановление из резервной копии
python3 -c "
from modules.registry_manager import get_registry_manager
manager = get_registry_manager()
manager.recover_registry_from_backup()
"

# Интерактивное восстановление
python3 -c "
from modules.registry_manager import emergency_recovery
emergency_recovery()
"
```

### Восстановление без реестра:
```bash
# Поиск бэкапов по манифестам
python3 -c "
from modules.registry_manager import get_registry_manager
manager = get_registry_manager()
found = manager.search_in_manifests('Метка_бэкапа')
for item in found:
    print(f'Найден: {item[\"label\"]} - {item[\"manifest\"]}')
"
```

## 📋 Лицензия и поддержка

### Лицензия
Система распространяется под лицензией MIT.

### Поддержка
- **Документация**: Этот файл README.md
- **Логи**: Директория `logs/` содержит всю информацию о работе системы
- **Тестирование**: Скрипты в директории `scripts/` для проверки компонентов

### Вклад в развитие
1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📞 Контакты и обратная связь

### Сообщение об ошибках
При обнаружении ошибок:
1. Проверьте логи в `logs/lto_errors.log`
2. Убедитесь, что уровень логирования установлен в DEBUG
3. Создайте issue с приложением соответствующих логов

### Вопросы и предложения
- Создавайте issue для вопросов и предложений
- Проверяйте документацию перед созданием issue
- Предоставляйте максимально подробную информацию о проблеме

---

**LTO Backup System** - профессиональное решение для надёжного резервного копирования на ленточные носители. 
Система прошла комплексное тестирование и готова к промышленной эксплуатации.

**🚀 Начните работу прямо сейчас:**
```bash
python3 lto_main.py status
python3 lto_main.py backup /важные/данные "Первый_бэкап"
```

**✅ Система проверена и готова к работе!**

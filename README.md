# LTO Backup System

Профессиональная система резервного копирования на ленту LTO-4 с поддержкой многотомности, YAML конфигурацией, Telegram уведомлениями и автоматическим планированием.

## ✨ Возможности

- **Резервное копирование на ленту** - Поддержка LTO-4 и совместимых накопителей
- **Многотомность** - Автоматическая смена лент через mbuffer
- **YAML конфигурация** - Читаемый и структурированный формат настроек
- **Telegram уведомления** - Уведомления о всех этапах бэкапа
- **Автоматическое планирование** - Ежедневные, еженедельные и ежемесячные бэкапы
- **Восстановление** - Легкое восстановление данных по метке бэкапа
- **Мониторинг** - Проверка состояния системы и оборудования
- **Поддержка роботов** - Автоматические ленточные библиотеки
- **Единый бинарный файл** - Все зависимости в одном исполняемом файле

## 🚀 Быстрый старт

### Установка из бинарного файла

```bash
# Скачайте бинарный файл
wget https://example.com/lto_backup
chmod +x lto_backup

# Установите в систему
sudo ./lto_backup --install
```

### Установка из исходников

```bash
# Клонируйте репозиторий
git clone https://github.com/your-repo/lto_backup.git
cd lto_backup

# Установите зависимости
pip install -r requirements.txt

# Или используйте setup.py
python3 setup.py
```

### Первая настройка

1. **Создайте конфигурацию:**
```bash
cp config.yaml.example config.yaml
nano config.yaml
```

2. **Настройте основные параметры:**
```yaml
hardware:
  tape_dev: "/dev/nst0"  # Ваше устройство ленты

telegram:
  enabled: true
  token: "ВАШ_ТОКЕН_БОТА"
  chat_id: "ВАШ_CHAT_ID"
```

3. **Проверьте систему:**
```bash
./lto_backup check
```

4. **Создайте первый бэкап:**
```bash
./lto_backup backup /важные/данные "Первый_бэкап_$(date +%Y%m%d)"
```

## ⚙️ Конфигурация

Основной файл конфигурации: `config.yaml`

### Основные секции

#### Оборудование
```yaml
hardware:
  tape_dev: "/dev/nst0"           # Устройство ленты
  has_robot: false                # Автоматическая библиотека
  robot_dev: "/dev/sg3"           # Устройство робота
  auto_rewind: true               # Автоперемотка после операций
```

#### Буферизация (mbuffer)
```yaml
mbuffer:
  size: "2G"                      # Размер буфера
  block_size: "256k"              # Размер блока
  fill_percent: "90%"             # Заполнение для смены ленты
  change_script: "lto_backup change_tape"  # Скрипт смены ленты
```

#### Telegram уведомления
```yaml
telegram:
  enabled: true                   # Включить уведомления
  token: "бот:токен"              # Токен от @BotFather
  chat_id: "123456789"            # ID чата или пользователя
  notification_level: "INFO"      # Уровень уведомлений
  backup_started: true            # Уведомлять о начале бэкапа
  backup_completed: true          # Уведомлять о завершении
  tape_change: true               # Уведомлять о смене ленты
```

#### Планирование
```yaml
scheduling:
  enabled: true                   # Включить автоматические бэкапы
  daily_at: "02:00"               # Ежедневно в 2:00
  weekly_day: "sunday"            # По воскресеньям
  monthly_day: 1                  # 1-го числа каждого месяца
  retention_policy:
    daily: 7                      # Хранить 7 ежедневных бэкапов
    weekly: 4                     # Хранить 4 еженедельных
    monthly: 12                   # Хранить 12 ежемесячных
```

## 📋 Команды

### Основные команды

| Команда | Описание | Пример |
|---------|----------|--------|
| `backup` | Создать бэкап | `lto_backup backup /data "Backup_2024"` |
| `restore` | Восстановить данные | `lto_backup restore /restore "Backup_2024"` |
| `list` | Список бэкапов | `lto_backup list` |
| `check` | Проверка системы | `lto_backup check` |
| `change_tape` | Сменить ленту | `lto_backup change_tape` |

### Утилиты

| Команда | Описание |
|---------|----------|
| `config` | Показать текущую конфигурацию |
| `stats` | Показать статистику системы |
| `validate` | Проверить валидность конфигурации |
| `version` | Показать версию системы |
| `scheduler` | Запустить планировщик |

## 🔧 Расширенные возможности

### Интеграция с mbuffer

Для автоматической смены лент настройте mbuffer в config.yaml:
```yaml
mbuffer:
  change_script: "lto_backup change_tape"
```

### Автоматические бэкапы

1. Включите планировщик в config.yaml:
```yaml
scheduling:
  enabled: true
  daily_at: "02:00"
```

2. Запустите планировщик:
```bash
lto_backup scheduler
```

Или используйте systemd:
```bash
sudo systemctl enable lto-backup
sudo systemctl start lto-backup
```

### Поддержка роботов ленточных библиотек

```yaml
hardware:
  has_robot: true
  robot_dev: "/dev/sg3"
```

### Исключение файлов

```yaml
exclude:
  patterns:
    - "/proc"
    - "/sys"
    - "/dev"
    - "*.log"
    - "*.tmp"
    - ".git"
  max_file_size: "10G"
  exclude_older_than: "365d"
```

## 📁 Структура проекта

```
lto_backup/
├── lto_backup.py              # Основное приложение
├── setup.py                   # Скрипт установки
├── build_binary.sh           # Скрипт сборки бинарника
├── config.yaml.example       # Пример конфигурации
├── requirements.txt          # Python зависимости
├── core/                    # Основные модули
│   ├── config_manager.py    # Менеджер конфигурации YAML
│   ├── backup_engine.py     # Движок бэкапа и восстановления
│   ├── registry_manager.py  # Управление реестром бэкапов
│   ├── tape_changer.py      # Скрипт смены ленты для mbuffer
│   └── scheduler.py         # Планировщик автоматических бэкапов
├── hardware/                # Аппаратные модули
│   ├── tape_driver.py       # Драйвер ленточного накопителя
│   └── robot_controller.py  # Контроллер робота библиотеки
├── notification/           # Уведомления
│   └── telegram_bot.py     # Telegram бот для уведомлений
└── utils/                 # Утилиты
    └── dependencies.py    # Проверка системных зависимостей
```

## 🛠️ Сборка бинарного файла

```bash
# Создание виртуального окружения и сборка
./build_binary.sh

# Бинарный файл будет создан в dist/lto_backup
```

### Установка бинарного файла

```bash
cd dist
sudo ./install.sh
```

Или вручную:
```bash
sudo cp lto_backup /usr/local/bin/
sudo chmod +x /usr/local/bin/lto_backup
sudo mkdir -p /etc/lto_backup
sudo cp config.yaml.example /etc/lto_backup/config.yaml
```

## 🐛 Устранение неисправностей

### Проблемы с доступом к устройству ленты

```bash
# Проверьте устройство
ls -la /dev/nst0

# Установите права
sudo chmod 666 /dev/nst0

# Или добавьте пользователя в группу tape
sudo usermod -a -G tape $USER
```

### Проблемы с зависимостями

```bash
# Ubuntu/Debian
sudo apt-get install tar mt-st mbuffer mtx-tools

# CentOS/RHEL
sudo yum install tar mt-st mbuffer mtx-tools

# Python зависимости
pip install -r requirements.txt
```

### Просмотр логов

```bash
# Системные логи
tail -f /var/log/lto_backup.log

# Логи смены ленты
tail -f /tmp/lto_tape_changer.log

# Логи планировщика
journalctl -u lto-backup -f
```

## 📊 Мониторинг

### Telegram уведомления

Система отправляет уведомления о:
- Начале и завершении бэкапа
- Ошибках операций
- Необходимости смены ленты
- Требовании чистки
- Результатах проверки системы

### Статистика

```bash
lto_backup stats
```

Показывает:
- Количество бэкапов
- Использованные ленты
- Статистику оборудования
- Дисковое пространство

## 🔒 Безопасность

### Конфигурация безопасности

```yaml
security:
  encrypt_backups: false        # Шифрование бэкапов
  gpg_key: ""                   # GPG ключ для шифрования
  hash_verification: true       # Проверка хешей файлов
  hash_algorithm: "sha256"      # Алгоритм хеширования
```

### Рекомендации по безопасности

1. Храните копию реестра бэкапов в безопасном месте
2. Используйте шифрование для конфиденциальных данных
3. Ограничьте доступ к конфигурационным файлам
4. Регулярно проверяйте логи на подозрительную активность

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 📞 Поддержка

- **Issues**: [GitHub Issues](https://github.com/your-repo/lto_backup/issues)
- **Документация**: [Wiki](https://github.com/your-repo/lto_backup/wiki)
- **Примеры конфигураций**: `config.yaml.example`

---

**LTO Backup System** - Надежное решение для резервного копирования на ленту с открытым исходным кодом.
